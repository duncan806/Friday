QUESTION MODE (round {{round}}).

You are the builder (Codex). You are blocked and may ask the task-holder ONE
short clarifying question about intended behavior or intent.

Hard rule: do NOT include code, file paths, diffs, or identifiers. Ask in plain
language only — anything code-shaped will be stripped before it reaches the
task-holder and logged as a leak.

Reports so far:
{{reports}}
